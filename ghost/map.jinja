{%- set ghost = salt['grains.filter_by']({
    'Debian': {'npm': 'npm',
              'node': 'nodejs-legacy',
              'unzip': 'unzip'}
    },
    merge=salt['pillar.get']('ghost:lookup'),
    default='Debian') %}
